[pytest]
testpaths = tests
addopts =
    -v
    --tb=short
    --cov=api
    --cov=model
    --cov=monitoring
    --cov-report=term-missing
    --cov-report=html:coverage_report
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
